export const dynamic = 'force-dynamic'

'use client'import { useState, useEffect } from 'react'import Link from 'next/link'import { createClientComponentClient } from '@supabase/auth-helpers-nextjs'import { useRouter } from 'next/navigation'export default function PagamentoPage() {  const [plano, setPlano] = useState<'mensal' | 'anual'>('mensal')  const [enviado, setEnviado] = useState(false)  const [arquivo, setArquivo] = useState<File | null>(null)  const supabase = createClientComponentClient()  const router = useRouter()  const handleEnviar = async (e: React.FormEvent) => {    e.preventDefault()    setEnviado(true)  }  return (    <div className="min-h-screen bg-gray-50">      <header className="bg-white shadow-sm border-b border-gray-200">        <div className="max-w-4xl mx-auto px-4 py-4 flex items-center gap-4">          <Link href="/dashboard" className="text-gray-500 hover:text-gray-700">← Dashboard</Link>          <h1 className="text-xl font-bold text-blue-900">Pagamento</h1>        </div>      </header>      <main className="max-w-2xl mx-auto px-4 py-8">        {enviado ? (          <div className="bg-white rounded-2xl shadow-sm border border-gray-200 p-8 text-center">            <div className="text-5xl mb-4">✅</div>            <h2 className="text-2xl font-bold text-green-700 mb-2">Comprovante enviado!</h2>            <p className="text-gray-600 mb-6">Seu pagamento será verificado e o plano ativado em até 24 horas.</p>            <Link href="/dashboard" className="bg-blue-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-blue-700 transition inline-block">              Voltar ao Dashboard
